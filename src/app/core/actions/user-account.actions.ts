import { createAction } from "@ngrx/store";

export const increment = createAction('[User Component] Increment');
export const decrement = createAction('[User Component] Decrement');
export const reset = createAction('[User Component] Reset');
